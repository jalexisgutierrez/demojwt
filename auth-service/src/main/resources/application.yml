server:
  port: 8080

spring:
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5433/authdb}
    username: ${DB_USER:auth}
    password: ${DB_PASS:auth}
  jpa:
    hibernate:
      ddl-auto: update

app:
  jwt:
    # ← valor por defecto para dev si no hay variable
    secret: ${APP_JWT_SECRET:p2P!Q7vH9mR@t5Zk1LwX8sC#bF&gN3yV*eU4oI6qD%jM7rA$hK2nO5wE8uT0lY!zB}
    expiration: ${APP_JWT_EXP_MS:21600000}
